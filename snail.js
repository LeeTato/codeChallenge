const snail = array=>{
    const middle = array.slice(1, array.length - 1).map(row =>
        row.slice(1, row.length -1)),
        array.length > 1 ? array=[arrar.length-1].reverse() : [],
        array.slice(1,)

}